interface iCurrentSale {
  currentSale: Object[];
  setCurrentSale: (data: Object[]) => void;
}

export const CartCard = () => {
  return;
  <div>
    <h1>teste</h1>;
  </div>;
};

export default CartCard;
